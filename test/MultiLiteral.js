// const { expectRevert } = require('openzeppelin-test-helpers');
// const { expect } = require('chai');

// const MultiLiteral = artifacts.require('MultiLiteral');

contract('MultiLiteral', function ([_, addr1]) {
    describe('MultiLiteral', async function () {
        it('should be ok', async function () {
            // this.token = await MultiLiteral.new();
        });
    });
});
